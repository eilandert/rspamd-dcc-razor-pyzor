*.log
*.tmp
# gozer source lives in its own repo, pulled in as the docker/gozer submodule;
# its built binary (docker/gozer/gozer) is ignored by that submodule's .gitignore
# and is only ever compiled inside the image build.
# local secrets (compose `secrets:` source) — never commit
secrets/
